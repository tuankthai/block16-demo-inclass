function rectangleArea(length, height) {
    // console.log(length*height)
    return length*height
    
    
}

const total = (rectangleArea(10, 10) + rectangleArea(10, 5) )*2

// function triangleArea(length, height) {
//     console.log(0.5*length * height)

// }

// triangleArea(3, 5)

console.log(total)
console.log(total)


//function expr
// variable = function expr
const rectangleAreaTwo = (length, height) => {
    // console.log(length*height)
    return length * height
}

//one liner function expr
const rectangleAreaThree = (parm1, parm2) => length * height
